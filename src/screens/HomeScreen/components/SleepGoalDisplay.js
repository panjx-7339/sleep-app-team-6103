import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SleepGoalDisplay = () => {
  return <Text>Today's Goal: 8 Hours</Text>;
};

export default SleepGoalDisplay;

const styles = StyleSheet.create({});
